[project]
name = "pwnpatterns-ci"
version = "0.0.0"
description = "CI utilities for PwnPatterns (metadata verification)"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = ["--strict-markers"]
